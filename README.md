# ctabs

- [x] code style: editor.config

## Using nvm ?

`nvm use`

# Migrations

lib node-pg-migrate

`npm run migration:create`
`npm run migration:up`
`npm run migration:down`

A conexão com o banco de dados é feita pelo arquivo .env.development que a liv usa ao executar o up
